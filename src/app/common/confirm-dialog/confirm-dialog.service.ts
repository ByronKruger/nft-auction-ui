import { computed, Injectable, signal } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ConfirmDialogService {
  // confirmImage = computed(() => this.confirmImage());
  // confirmQuestion = computed(() => this.confirmQuestion());
  // confirmText = computed(() => this.confirmText());
  // cancelText = computed(() => this.cancelText());
  // confirmImage = signal<string>("");
  // confirmQuestion = signal<string>("");
  // confirmText = signal<string>("");
  // cancelText = signal<string>("");

  constructor() { }
}
